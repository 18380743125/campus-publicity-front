import React, { memo } from 'react'
import { InformationWrapper } from './style'

interface Props {}

const Information = memo((props: Props) => {
  return <InformationWrapper>informationWrapper</InformationWrapper>
})

export default Information
